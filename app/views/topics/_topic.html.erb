
  <%= link_to topic.title, topic_path(topic), class: 'list-group-item list-group-item-action list-group-item-light'%>
  <%
=begin%>
 <%= link_to fa_icon('pencil-square-o'), edit_topic_path(@topic) if logged_in?(:site_admin) %> 
<%
=end%>
